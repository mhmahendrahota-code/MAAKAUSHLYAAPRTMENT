# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret
JWT_SECRET=super_secret_key_change_me_in_production
JWT_EXPIRES_IN=7d

# Database Configuration (PostgreSQL)
DATABASE_URL=postgres://postgres:password@localhost:5432/makaushlya_apartment

# Fallback In-Memory/JSON Storage (True to bypass strict database connection errors)
DATABASE_FALLBACK=true
